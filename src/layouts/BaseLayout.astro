---
import '../styles/tokens.css';
import CursorTrailGrid from '../components/CursorTrailGrid.astro';
import CursorTrail from '../components/CursorTrail.astro';
import CustomCursor from '../components/CustomCursor.astro';

interface Props {
	title?: string;
}

const { title = 'Astro' } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />

		<!-- Google Fonts: Micro 5 (titles), Roboto (body), Space Mono (article code blocks) -->
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Micro+5&family=Roboto:wght@400;500;700&family=Space+Mono:ital,wght@0,400;0,700;1,400&display=swap"
			rel="stylesheet"
		/>

		<title>{title}</title>
		<style is:global>
			html,
			body {
				margin: 0;
				padding: 0;
			}

			html {
				scroll-behavior: smooth;
			}

			body {
				background: var(--page-bg, #1a1a2e);
			}

			@media (max-width: 800px) {
				body {
					background: #0d1316; /* Match content area — no visible teal gap when URL bar collapses */
				}
				.custom-cursor {
					display: none !important;
				}
			}

			.base-layout-wrapper {
				position: relative;
				z-index: 1;
			}
		</style>
	</head>
	<body>
		<div class="base-layout-wrapper">
			<CursorTrailGrid />
			<CursorTrail />
			<slot />
		</div>
		<CustomCursor />
	</body>
</html>
